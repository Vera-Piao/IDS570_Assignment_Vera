# IDS570_Assignment_Vera

## Dataset

The dataset `ADHD-comment.csv.zip` (507MB) is tracked using Git LFS.

To clone this repository properly:

1. Install Git LFS
2. Run:
   git lfs install
3. Clone the repository:
   git clone <repo_url>
